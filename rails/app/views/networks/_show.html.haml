= form_for :network, :'data-remote' => true, :url => network_path(network.id), :html => { :method=>:put, :'data-type' => 'html',  :class => "formtastic" } do |f|
  %dl
    %dt= t '.name'
    %dd= link_to network.name, role_path(network.role.id)
    %dt= t '.description'
    %dd= text_field_tag :description, network.description, :size=>30
    %dt= t '.deployment'
    %dd= select_tag :deployment_id, options_from_collection_for_select(Deployment.all, :id, :name, :selected=>network.deployment.id)
    %dt= t '.conduit'
    %dd= text_field_tag :conduit, network.conduit, :size=>15
    %dt= t '.vlan'
    %dd= text_field_tag :vlan, network.vlan, :size=>5
    %dt= t '.v6prefix'
    %dd= text_field_tag :v6prefix, network.v6prefix, :size=>20
    %dt= t '.team'
    %dd= text_field_tag :team_mode, network.team_mode, :size=>5
    %dt= t '.bridge'
    %dd= text_field_tag :use_bridge, network.use_bridge, :size=>5
    %dt
    %dd
      %input.button{:type => "submit", :value => t('update')}

